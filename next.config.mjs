/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "localhost",
            "behemoth-shop-production.up.railway.app",
        ]
    }
};

export default nextConfig;
